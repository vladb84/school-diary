// TODO: вставить полный код App.jsx из Claude
//
// Замены для работы вне Claude.ai:
//   window.storage.get(key)      →  { value: localStorage.getItem(key) }
//   window.storage.set(key, val) →  localStorage.setItem(key, val)
